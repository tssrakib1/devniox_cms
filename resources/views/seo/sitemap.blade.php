{!! '<'.'?xml version="1.0" encoding="UTF-8"?>' !!}<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">@foreach(['home','about','services','products','portfolio','blog','contact'] as $route)<url><loc>{{ route($route) }}</loc></url>@endforeach @foreach($sitemapProducts as $item)<url><loc>{{ route('products.show',$item->slug) }}</loc><lastmod>{{ $item->updated_at->toAtomString() }}</lastmod></url>@endforeach @foreach($sitemapServices as $item)<url><loc>{{ route('services.show',$item->slug) }}</loc><lastmod>{{ $item->updated_at->toAtomString() }}</lastmod></url>@endforeach @foreach($sitemapPortfolio as $item)<url><loc>{{ route('portfolio.show',$item->slug) }}</loc><lastmod>{{ $item->updated_at->toAtomString() }}</lastmod></url>@endforeach @foreach($sitemapPosts as $item)<url><loc>{{ route('blog.show',$item->slug) }}</loc><lastmod>{{ $item->updated_at->toAtomString() }}</lastmod></url>@endforeach @foreach($sitemapBlogCategories as $item)<url><loc>{{ route('blog',['category'=>$item->slug]) }}</loc><lastmod>{{ $item->updated_at->toAtomString() }}</lastmod></url>@endforeach @foreach($sitemapBlogTags as $item)<url><loc>{{ route('blog',['tag'=>$item->slug]) }}</loc><lastmod>{{ $item->updated_at->toAtomString() }}</lastmod></url>@endforeach</urlset>
