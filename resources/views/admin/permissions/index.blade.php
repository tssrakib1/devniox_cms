@extends('layouts.admin') @section('title','Permissions') @section('heading','Permissions') @section('content')<div class="card"><div class="card-header"><strong>Module-level permission matrix</strong><p class="text-muted mb-0">Permissions are assigned from each role's edit page.</p></div><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Module</th><th>Action</th><th>Assigned roles</th></tr></thead><tbody>@foreach($permissions as $module=>$items)@foreach($items as $permission)<tr><td class="text-capitalize">{{ str_replace('-',' ',$module) }}</td><td class="text-capitalize">{{ $permission->action }}</td><td>{{ $permission->roles->pluck('name')->join(', ') ?: 'None' }}</td></tr>@endforeach @endforeach</tbody></table></div></div>@endsection
