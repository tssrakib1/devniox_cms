@extends('layouts.admin') @section('title','Roles') @section('heading','Roles') @section('content')<div class="d-flex justify-content-end gap-2 mb-3">@can('access','roles.view')<a class="btn btn-outline-secondary" href="{{ route('admin.permissions.index') }}">Permission matrix</a>@endcan @can('access','roles.create')<a class="btn btn-primary" href="{{ route('admin.roles.create') }}">Add role</a>@endcan</div><div class="card"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Role</th><th>Type</th><th>Users</th><th class="text-end">Actions</th></tr></thead><tbody>@foreach($roles as $role)<tr><td><strong>{{ $role->name }}</strong><div class="small text-muted">{{ $role->slug }}</div></td><td>{{ $role->is_system ? 'System' : 'Custom' }}</td><td>{{ $role->users_count }}</td><td class="text-end">@can('access','roles.edit')<a class="btn btn-sm btn-outline-primary" href="{{ route('admin.roles.edit',$role) }}">Edit</a>@endcan @can('access','roles.delete')@unless($role->is_system)<form class="d-inline" method="post" action="{{ route('admin.roles.destroy',$role) }}" onsubmit="return confirm('Delete this unused role?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" @disabled($role->users_count>0)>Delete</button></form>@endunless @endcan</td></tr>@endforeach</tbody></table></div></div>@endsection
