@extends('layouts.admin')
@section('title',str($type)->headline().' Categories')
@section('heading',str($type)->headline().' Categories')
@section('content')
@php $routeBase='admin.finance.'.$type.'-categories'; @endphp
<div class="d-flex justify-content-between align-items-center mb-3"><p class="text-body-secondary mb-0">Organize {{ $type }} transactions for consistent reporting.</p><a class="btn btn-primary" href="{{ route($routeBase.'.create') }}"><i class="bi bi-plus-lg" aria-hidden="true"></i> Add Category</a></div><div class="card shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Name</th><th>Slug</th><th>Status</th><th>Transactions</th><th>Updated</th><th class="text-end">Actions</th></tr></thead><tbody>@forelse($categories as $category)<tr><td><strong>{{ $category->name }}</strong><br><small class="text-body-secondary">{{ str($category->description)->limit(80) }}</small></td><td><code>{{ $category->slug }}</code></td><td><span class="badge {{ $category->active?'text-bg-success':'text-bg-secondary' }}">{{ $category->active?'Active':'Inactive' }}</span></td><td>{{ $category->transactions_count }}</td><td>{{ $category->updated_at->diffForHumans() }}</td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route($routeBase.'.edit',$category) }}">Edit</a>@can('delete',$category)<form class="d-inline" method="POST" action="{{ route($routeBase.'.destroy',$category) }}" data-confirm="Delete this category?">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form>@endcan</td></tr>@empty<tr><td colspan="6"><x-empty-state title="No categories found"/></td></tr>@endforelse</tbody></table></div>@if($categories->hasPages())<div class="card-footer">{{ $categories->links() }}</div>@endif</div>
@endsection
