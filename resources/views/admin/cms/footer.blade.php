@extends('layouts.admin') @section('title','Footer') @section('heading','Footer Editor') @section('content')<form method="post" action="{{ route('admin.cms.footer.update') }}">@csrf @method('PUT')<x-card title="Footer content"><div class="row g-3">@foreach(['copyright','short_description','quick_links_heading','products_heading','services_heading','blog_heading','privacy_url','terms_url','cookies_url'] as $field)<div class="col-md-6"><label class="form-label">{{ Str::headline($field) }}</label><input class="form-control" name="{{ $field }}" value="{{ old($field,$footer->{$field}) }}" @if(!str_ends_with($field,'url')) required @endif></div>@endforeach</div></x-card><button class="btn btn-primary mt-4">Save footer</button></form>@endsection
