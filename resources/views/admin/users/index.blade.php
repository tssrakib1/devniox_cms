@extends('layouts.admin')
@section('title','Users') @section('heading','Users')
@section('content')
<div class="card"><div class="card-header d-flex flex-wrap gap-2 align-items-center"><form class="d-flex gap-2 me-auto" method="get" role="search"><label class="visually-hidden" for="user-search">Search users</label><input class="form-control" id="user-search" name="search" value="{{ request('search') }}" placeholder="Name or email"><button class="btn btn-outline-secondary">Search</button></form>@can('access','users.create')<a class="btn btn-primary" href="{{ route('admin.users.create') }}"><i class="bi bi-plus-lg"></i> Add user</a>@endcan</div><div class="table-responsive"><table class="table mb-0"><thead><tr><th>User</th><th>Role</th><th>Status</th><th>Created</th><th class="text-end">Actions</th></tr></thead><tbody>@forelse($users as $user)<tr><td><strong>{{ $user->name }}</strong><div class="small text-muted">{{ $user->email }}</div></td><td>{{ $user->managedRole?->name ?? ucfirst($user->role->value) }}</td><td><span class="badge text-bg-{{ $user->is_active ? 'success' : 'secondary' }}">{{ $user->is_active ? 'Active' : 'Inactive' }}</span></td><td>{{ $user->created_at->format('M j, Y') }}</td><td class="text-end text-nowrap">@can('access','users.edit')<a class="btn btn-sm btn-outline-primary" href="{{ route('admin.users.edit',$user) }}">Edit</a>@endcan @can('access','users.delete')<form class="d-inline" method="post" action="{{ route('admin.users.destroy',$user) }}" onsubmit="return confirm('Delete this user?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" @disabled(auth()->id()===$user->id)>Delete</button></form>@endcan</td></tr>@empty<tr><td colspan="5"><x-empty-state title="No users found">Create a user or adjust your search.</x-empty-state></td></tr>@endforelse</tbody></table></div>@if($users->hasPages())<div class="card-footer">{{ $users->links() }}</div>@endif</div>
@endsection
