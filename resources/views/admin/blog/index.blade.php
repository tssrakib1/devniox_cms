@extends('layouts.admin') @section('title','Blog') @section('heading','Blog') @section('content')<form class="row g-2 mb-3" method="get"><div class="col"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Search posts"></div><div class="col"><select class="form-select" name="status"><option value="">All statuses</option>@foreach(['draft','scheduled','published','archived'] as $status)<option @selected(request('status')===$status)>{{ $status }}</option>@endforeach</select></div><div class="col-auto"><button class="btn btn-outline-primary">Filter</button><a class="btn btn-primary" href="{{ route('admin.blog.create') }}">New post</a></div></form><form method="post" action="{{ route('admin.blog.bulk') }}">@csrf<div class="d-flex gap-2 mb-2"><select class="form-select w-auto" name="action">@foreach(['publish','draft','schedule','archive','delete','restore','feature','unfeature'] as $a)<option value="{{ $a }}">{{ ucfirst($a) }}</option>@endforeach</select><button class="btn btn-secondary">Apply</button></div><div class="card table-responsive"><table class="table align-middle mb-0"><thead><tr><th></th><th>Title</th><th>Category</th><th>Author</th><th>Status</th><th>Views</th><th>Updated</th><th></th></tr></thead><tbody>@forelse($posts as $post)<tr><td><input type="checkbox" name="post_ids[]" value="{{ $post->id }}" aria-label="Select {{ $post->title }}"></td><td><strong>{{ $post->title }}</strong></td><td>{{ $post->category->name }}</td><td>{{ $post->author->name }}</td><td>{{ ucfirst($post->status->value) }}</td><td>{{ $post->views_count }}</td><td>{{ $post->updated_at->diffForHumans() }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.blog.edit',$post) }}">Edit</a></td></tr>@empty<tr><td colspan="8"><x-empty-state title="No posts found"/></td></tr>@endforelse</tbody></table></div></form><div class="mt-3">{{ $posts->links() }}</div>@endsection
