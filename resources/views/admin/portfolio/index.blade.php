@extends('layouts.admin') @section('title','Portfolio') @section('heading','Portfolio') @section('content')<form class="row g-2 mb-3" method="get"><div class="col-lg"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Search projects" aria-label="Search projects"></div><div class="col-lg"><select class="form-select" name="category" aria-label="Category"><option value="">All categories</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected((string)request('category')===(string)$category->id)>{{ $category->name }}</option>@endforeach</select></div><div class="col-lg"><select class="form-select" name="status" aria-label="Status"><option value="">All statuses</option>@foreach(['draft','published','archived'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select></div><div class="col-lg"><select class="form-select" name="featured" aria-label="Featured"><option value="">All</option><option value="1" @selected(request('featured')==='1')>Featured</option><option value="0" @selected(request('featured')==='0')>Not featured</option></select></div><div class="col-auto"><label class="form-check mt-2"><input class="form-check-input" type="checkbox" name="trashed" value="1" @checked(request('trashed')==='1')> Deleted</label></div><div class="col-auto"><button class="btn btn-outline-primary">Filter</button></div><div class="col-auto"><a class="btn btn-primary" href="{{ route('admin.portfolio.create') }}">New project</a></div></form><form method="post" action="{{ route('admin.portfolio.bulk') }}">@csrf<div class="d-flex gap-2 mb-2"><select class="form-select w-auto" name="action" required aria-label="Bulk action"><option value="">Bulk action</option>@foreach(['publish','draft','archive','delete','restore','feature','unfeature'] as $action)<option value="{{ $action }}">{{ ucfirst($action) }}</option>@endforeach</select><button class="btn btn-secondary">Apply</button></div><div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th><input type="checkbox" data-select-all aria-label="Select all"></th><th>Image</th><th>Project</th><th>Category</th><th>Client</th><th>Status</th><th>Featured</th><th>Updated</th><th class="text-end">Actions</th></tr></thead><tbody>@forelse($projects as $project)<tr><td><input type="checkbox" name="project_ids[]" value="{{ $project->id }}" aria-label="Select {{ $project->name }}"></td><td>@if($project->thumbnail_path)<img width="64" height="44" class="rounded object-fit-cover" src="{{ Storage::url($project->thumbnail_path) }}" alt="">@endif</td><td><strong>{{ $project->name }}</strong></td><td>{{ $project->category->name }}</td><td>{{ $project->client_name?:'—' }}</td><td>{{ ucfirst($project->status->value) }}</td><td>{{ $project->is_featured?'Yes':'No' }}</td><td>{{ $project->updated_at->diffForHumans() }}</td><td class="text-end">@if(!$project->trashed())<a class="btn btn-sm btn-outline-secondary" href="{{ route('admin.portfolio.show',$project) }}">View</a><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.portfolio.edit',$project) }}">Edit</a>@endif</td></tr>@empty<tr><td colspan="9"><x-empty-state title="No portfolio projects found"/></td></tr>@endforelse</tbody></table></div></div></form><div class="mt-3">{{ $projects->links() }}</div>@endsection
