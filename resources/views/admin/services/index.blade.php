@extends('layouts.admin') @section('title','Services') @section('heading','Services')
@section('content')<form class="row g-2 mb-3" method="get"><div class="col-lg"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Search services" aria-label="Search services"></div><div class="col-lg"><select class="form-select" name="category" aria-label="Category"><option value="">All categories</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected((string)request('category')===(string)$category->id)>{{ $category->name }}</option>@endforeach</select></div><div class="col-lg"><select class="form-select" name="status" aria-label="Status"><option value="">All statuses</option>@foreach(['draft','published','archived'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select></div><div class="col-auto"><button class="btn btn-outline-primary">Filter</button></div><div class="col-auto"><a class="btn btn-primary" href="{{ route('admin.services.create') }}">New service</a></div></form>
<form method="post" action="{{ route('admin.services.bulk') }}">@csrf<div class="d-flex gap-2 mb-2"><select class="form-select w-auto" name="action" required aria-label="Bulk action"><option value="">Bulk action</option>@foreach(['publish','draft','archive','delete','feature','unfeature'] as $action)<option value="{{ $action }}">{{ ucfirst($action) }}</option>@endforeach</select><button class="btn btn-secondary">Apply</button></div><div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th><input type="checkbox" data-select-all aria-label="Select all"></th><th>Image</th><th>Name</th><th>Category</th><th>Status</th><th>Featured</th><th>Updated</th><th class="text-end">Actions</th></tr></thead><tbody>@forelse($services as $service)<tr><td><input type="checkbox" name="service_ids[]" value="{{ $service->id }}" aria-label="Select {{ $service->name }}"></td><td>@if($service->cover_image_path)<img src="{{ Storage::url($service->cover_image_path) }}" width="64" height="44" class="rounded object-fit-cover" alt="">@endif</td><td><strong>{{ $service->name }}</strong><br><small>{{ $service->slug }}</small></td><td>{{ $service->category->name }}</td><td><span class="badge text-bg-{{ $service->status->value==='published'?'success':'secondary' }}">{{ ucfirst($service->status->value) }}</span></td><td>{{ $service->is_featured?'Yes':'No' }}</td><td>{{ $service->updated_at->diffForHumans() }}</td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.services.edit',$service) }}">Edit</a></td></tr>@empty<tr><td colspan="8"><x-empty-state title="No services found" /></td></tr>@endforelse</tbody></table></div></div></form><div class="mt-3">{{ $services->links() }}</div>@endsection
