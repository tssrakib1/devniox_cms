@extends('layouts.admin')
@section('title','Communication #'.$lead->id)
@section('heading','Communication #'.$lead->id)
@section('content')
<div class="row g-4"><div class="col-xl-8">
<x-card title="Contact information"><dl class="row mb-0"><dt class="col-sm-4">Type</dt><dd class="col-sm-8">{{ Str::headline($lead->type->value) }}</dd><dt class="col-sm-4">Name</dt><dd class="col-sm-8">{{ $lead->name }}</dd><dt class="col-sm-4">Company</dt><dd class="col-sm-8">{{ $lead->company?:'—' }}</dd><dt class="col-sm-4">Email</dt><dd class="col-sm-8"><a href="mailto:{{ $lead->email }}">{{ $lead->email }}</a></dd><dt class="col-sm-4">Phone</dt><dd class="col-sm-8">@if($lead->phone)<a href="tel:{{ $lead->phone }}">{{ $lead->phone }}</a>@else—@endif</dd><dt class="col-sm-4">Received</dt><dd class="col-sm-8">{{ $lead->submitted_at->format('M j, Y g:i A') }}</dd></dl></x-card>
<x-card title="Request details" class="mt-4">@if($lead->contactMessage)<dl><dt>Subject</dt><dd>{{ $lead->contactMessage->subject }}</dd></dl><p class="text-break">{!! nl2br(e($lead->contactMessage->message)) !!}</p>@elseif($lead->demoRequest)<dl class="row"><dt class="col-sm-4">Interested product/service</dt><dd class="col-sm-8">{{ $lead->demoRequest->item?->name?:'Unavailable' }}</dd><dt class="col-sm-4">Preferred schedule</dt><dd class="col-sm-8">{{ $lead->demoRequest->preferred_date?->format('M j, Y')?:'Flexible' }} {{ $lead->demoRequest->preferred_time?:'' }}</dd><dt class="col-sm-4">Meeting type</dt><dd class="col-sm-8">{{ Str::headline($lead->demoRequest->meeting_type) }}</dd></dl><p>{!! nl2br(e($lead->demoRequest->message?:'No additional message.')) !!}</p>@elseif($lead->quoteRequest)<dl class="row"><dt class="col-sm-4">Business type</dt><dd class="col-sm-8">{{ $lead->quoteRequest->business_type?:'Not specified' }}</dd><dt class="col-sm-4">Interested product</dt><dd class="col-sm-8">{{ $lead->quoteRequest->item?->name?:'Unavailable' }}</dd><dt class="col-sm-4">Budget</dt><dd class="col-sm-8">{{ $lead->quoteRequest->budget?:'Not specified' }}</dd><dt class="col-sm-4">Expected timeline</dt><dd class="col-sm-8">{{ $lead->quoteRequest->timeline?:'Not specified' }}</dd></dl><p>{!! nl2br(e($lead->quoteRequest->requirement_details)) !!}</p>@if($lead->quoteRequest->attachment_path)<a class="btn btn-outline-secondary" href="{{ route('admin.leads.attachment',$lead) }}"><i class="bi bi-download" aria-hidden="true"></i> Download original attachment</a>@endif @endif</x-card>
<x-card title="Reply history" class="mt-4"><form method="post" action="{{ route('admin.leads.replies.store',$lead) }}" enctype="multipart/form-data">@csrf<div class="mb-3"><label class="form-label" for="reply_subject">Email subject <span class="text-danger">*</span></label><input class="form-control" id="reply_subject" name="subject" required maxlength="200" value="{{ old('subject','Re: '.($lead->contactMessage?->subject?:'Your DevNiox enquiry')) }}"></div><div class="mb-3"><label class="form-label" for="reply_message">Message <span class="text-danger">*</span></label><textarea class="form-control" id="reply_message" name="message" rows="5" required maxlength="30000">{{ old('message') }}</textarea></div><div class="mb-3"><label class="form-label" for="reply_files">Attachments</label><input class="form-control" id="reply_files" type="file" name="attachments[]" multiple accept=".pdf,.doc,.docx,.png,.jpg,.jpeg,.webp,.zip"><div class="form-text">PDF, DOCX, images or ZIP. Maximum 10 MB each.</div></div><button class="btn btn-primary"><i class="bi bi-reply" aria-hidden="true"></i> Record reply</button></form><hr>@forelse($lead->replies as $reply)<article class="border-start border-3 border-primary ps-3 mb-3"><div class="d-flex justify-content-between gap-3"><strong>{{ $reply->subject }}</strong><time class="small text-muted">{{ $reply->replied_at->format('M j, Y g:i A') }}</time></div><p class="my-2">{!! nl2br(e($reply->message)) !!}</p><small>{{ $reply->administrator?->name?:'System' }}</small>@foreach($reply->attachments as $attachment)<a class="badge text-bg-light ms-2" href="{{ route('admin.leads.attachments.download',[$lead,$attachment]) }}"><i class="bi bi-paperclip"></i> {{ $attachment->original_name }}</a>@endforeach</article>@empty<p class="text-muted">No replies recorded.</p>@endforelse</x-card>
<x-card title="Private attachments" class="mt-4"><form class="row g-2" method="post" action="{{ route('admin.leads.attachments.store',$lead) }}" enctype="multipart/form-data">@csrf<div class="col-md-4"><label class="form-label" for="attachment_label">Label</label><input class="form-control" id="attachment_label" name="label" maxlength="160"></div><div class="col-md-6"><label class="form-label" for="attachment_file">File</label><input class="form-control" id="attachment_file" type="file" name="file" required accept=".pdf,.doc,.docx,.png,.jpg,.jpeg,.webp,.zip"></div><div class="col-md-2 align-self-end"><button class="btn btn-outline-primary w-100">Upload</button></div></form><div class="table-responsive mt-3"><table class="table table-sm align-middle"><thead><tr><th>File</th><th>Size</th><th>Uploaded by</th><th></th></tr></thead><tbody>@forelse($lead->attachments->whereNull('communication_reply_id') as $attachment)<tr><td><a href="{{ route('admin.leads.attachments.download',[$lead,$attachment]) }}">{{ $attachment->label?:$attachment->original_name }}</a></td><td>{{ number_format($attachment->file_size/1024,1) }} KB</td><td>{{ $attachment->uploader?->name?:'System' }}</td><td class="text-end"><form class="d-inline" method="post" action="{{ route('admin.leads.attachments.destroy',[$lead,$attachment]) }}" data-confirm="Remove this attachment?">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" aria-label="Remove {{ $attachment->original_name }}"><i class="bi bi-trash"></i></button></form></td></tr>@empty<tr><td colspan="4" class="text-muted">No private attachments.</td></tr>@endforelse</tbody></table></div></x-card>
<x-card title="Internal notes" class="mt-4"><form method="post" action="{{ route('admin.leads.notes.store',$lead) }}">@csrf<label class="form-label" for="note">Add note</label><textarea class="form-control" id="note" name="note" rows="3" required maxlength="10000"></textarea><button class="btn btn-primary mt-2">Add note</button></form><hr>@forelse($lead->notes as $note)<article class="mb-3"><p class="mb-1">{!! nl2br(e($note->note)) !!}</p><small class="text-muted">{{ $note->author->name }} · {{ $note->created_at->diffForHumans() }}</small></article>@empty<p class="text-muted">No internal notes.</p>@endforelse</x-card>
<x-card title="Timeline" class="mt-4"><ol class="list-group list-group-numbered">@foreach($lead->events as $event)<li class="list-group-item"><strong>{{ Str::headline($event->event_type) }}</strong> — {{ $event->description }}<br><small class="text-muted">{{ $event->actor?->name?:'System' }} · {{ $event->occurred_at->format('M j, Y g:i A') }}</small></li>@endforeach</ol></x-card></div>
<div class="col-xl-4"><x-card title="Communication actions"><form method="post" action="{{ route('admin.leads.update',$lead) }}">@csrf @method('PUT')<label class="form-label" for="status">Status</label><select class="form-select mb-3" id="status" name="status">@foreach($statuses as $status)<option value="{{ $status->value }}" @selected($lead->status===$status)>{{ Str::headline($status->value) }}</option>@endforeach</select><label class="form-label" for="priority">Priority</label><select class="form-select mb-3" id="priority" name="priority">@foreach($priorities as $priority)<option value="{{ $priority->value }}" @selected($lead->priority===$priority)>{{ Str::headline($priority->value) }}</option>@endforeach</select><label class="form-label" for="assigned_to">Assigned administrator</label><select class="form-select mb-3" id="assigned_to" name="assigned_to"><option value="">Unassigned</option>@foreach($administrators as $admin)<option value="{{ $admin->id }}" @selected($lead->assigned_to===$admin->id)>{{ $admin->name }}</option>@endforeach</select><button class="btn btn-primary">Update communication</button></form>@can('delete',$lead)<hr><form method="post" action="{{ route('admin.leads.destroy',$lead) }}" data-confirm="Archive this communication?">@csrf @method('DELETE')<button class="btn btn-outline-danger">Archive</button></form>@endcan</x-card>
@if($lead->type===\App\Enums\LeadType::Quote)<x-card title="Quote conversion" class="mt-4">@if($lead->convertedOrder)<p>Converted to <a href="{{ route('admin.orders.show',$lead->convertedOrder) }}">{{ $lead->convertedOrder->order_number }}</a>.</p>@else<form method="post" action="{{ route('admin.leads.convert',$lead) }}">@csrf<label class="form-label" for="item_name">Order item</label><input class="form-control mb-3" id="item_name" name="item_name" required maxlength="200" value="{{ old('item_name',$lead->item?->name?:'Custom software project') }}"><label class="form-label" for="item_type">Item type</label><select class="form-select mb-3" id="item_type" name="item_type">@foreach(\App\Enums\OrderItemType::cases() as $type)<option value="{{ $type->value }}">{{ Str::headline($type->value) }}</option>@endforeach</select><label class="form-label" for="amount">Agreed amount</label><input class="form-control mb-3" id="amount" type="number" name="amount" min="0" step="0.01" required><label class="form-label" for="expected_delivery_date">Expected delivery</label><input class="form-control mb-3" id="expected_delivery_date" type="date" name="expected_delivery_date" min="{{ today()->toDateString() }}"><button class="btn btn-success" data-confirm="Convert this quote to an order?">Convert to order</button></form>@endif</x-card>@endif
<x-card title="Submission context" class="mt-4"><dl><dt>IP address</dt><dd>{{ $lead->ip_address?:'Unavailable' }}</dd><dt>Browser</dt><dd class="text-break small">{{ $lead->user_agent?:'Unavailable' }}</dd><dt>Read</dt><dd>{{ $lead->read_at?->format('M j, Y g:i A')?:'Unread' }}</dd><dt>Last reply</dt><dd>{{ $lead->replied_at?->format('M j, Y g:i A')?:'No reply' }}</dd></dl></x-card></div></div>
@endsection
