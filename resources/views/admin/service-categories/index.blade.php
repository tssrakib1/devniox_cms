@extends('layouts.admin') @section('title','Service Categories') @section('heading','Service Categories')
@section('content')<div class="d-flex justify-content-between gap-3 mb-3"><form class="d-flex gap-2" method="get"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Search categories" aria-label="Search categories"><select class="form-select" name="status" aria-label="Status"><option value="">All statuses</option><option value="draft" @selected(request('status')==='draft')>Draft</option><option value="published" @selected(request('status')==='published')>Published</option></select><label class="form-check align-self-center text-nowrap"><input class="form-check-input" type="checkbox" name="trashed" value="1" @checked(request('trashed')==='1')> Deleted</label><button class="btn btn-outline-primary">Filter</button></form><a class="btn btn-primary align-self-start" href="{{ route('admin.service-categories.create') }}">New category</a></div><div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Name</th><th>Status</th><th>Services</th><th>Order</th><th class="text-end">Actions</th></tr></thead><tbody>@forelse($categories as $category)<tr><td><strong>{{ $category->name }}</strong><br><small class="text-muted">{{ $category->slug }}</small></td><td><span class="badge text-bg-{{ $category->status->value==='published'?'success':'secondary' }}">{{ ucfirst($category->status->value) }}</span></td><td>{{ $category->services_count }}</td><td>{{ $category->sort_order }}</td><td class="text-end">@if($category->trashed())<form class="d-inline" method="post" action="{{ route('admin.service-categories.restore',$category->id) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-success">Restore</button></form>@else<a class="btn btn-sm btn-outline-primary" href="{{ route('admin.service-categories.edit',$category) }}">Edit</a><form class="d-inline" method="post" action="{{ route('admin.service-categories.destroy',$category) }}" data-confirm="Delete this category?">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form>@endif</td></tr>@empty<tr><td colspan="5"><x-empty-state title="No categories found" /></td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $categories->links() }}</div>@endsection
