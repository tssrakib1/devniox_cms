@extends('layouts.app')
@section('title',$cmsPage->meta_title?:($siteSettings['seo.meta_title']??$cmsPage->contact->hero_heading))
@section('description',$cmsPage->meta_description?:($siteSettings['seo.meta_description']??$cmsPage->contact->hero_description))
@section('canonical',$cmsPage->canonical_url?:route('contact', [], false))
@push('head')@unless($cmsPage->is_indexable)<meta name="robots" content="noindex,nofollow">@endunless @endpush
@section('content')
@php
    $contact = $cmsPage->contact;
    $copy = static fn ($value, $fallback, array $seedCopy = []) => blank($value) || in_array($value, $seedCopy, true) ? $fallback : $value;
    $lines = static fn ($value, array $fallback) => collect(preg_split('/\r\n|\r|\n/', blank($value) ? implode("\n", $fallback) : $value))->filter(fn ($line) => filled(trim($line)))->values();
    $companyName = $siteSettings['contact.company_name'] ?? $contact->company_name;
    $heroHeading = $copy($contact->hero_heading, 'Start with the business outcome-not a feature list.', ['Let’s build something useful.']);
    $heroDescription = $copy($contact->hero_description, 'Contact DevNiox when an operational workflow needs stronger control, a software product needs evaluation, or an existing system is limiting growth. Share the context and we will identify the most useful next conversation.', ['Tell us what you are working on.']);
    $heroLabel = $contact->hero_label ?: 'Start a conversation';
    $emailLabel = $contact->email_card_label ?: 'Email';
    $phoneLabel = $contact->phone_card_label ?: 'Call';
    $whatsappLabel = $contact->whatsapp_card_label ?: 'WhatsApp';
    $inquiryLabel = $contact->inquiry_card_label ?: 'Business inquiry';
    $closedLabel = $contact->closed_label ?: 'Closed';
    $guidance = [
        ['number' => '01', 'title' => $contact->guidance_one_title ?: 'Business inquiry', 'description' => $contact->guidance_one_description ?: 'Use the contact form for workflow problems, integration questions, delivery partnerships, or an initial technical discussion.', 'helper' => $contact->guidance_one_helper_title ?: 'Helpful context', 'items' => $lines($contact->guidance_one_helper_items, ['Business objective and affected teams', 'Current process or system', 'Constraints, integrations, and timing'])],
        ['number' => '02', 'title' => $contact->guidance_two_title ?: 'Product demo', 'description' => $contact->guidance_two_description ?: 'Select the product or system you are evaluating. The session is organised around your roles, controls, reports, and deployment questions.', 'helper' => $contact->guidance_two_helper_title ?: 'What happens next', 'items' => $lines($contact->guidance_two_helper_items, ['We confirm the evaluation context', 'The demonstration follows relevant workflows', 'Open fit and implementation questions are recorded']), 'url' => route('demo-request'), 'link' => $contact->guidance_two_link_text ?: 'Request a demo'],
        ['number' => '03', 'title' => $contact->guidance_three_title ?: 'Quote request', 'description' => $contact->guidance_three_description ?: 'Use the quote workflow when the scope is sufficiently clear to discuss commercial options, delivery assumptions, and dependencies.', 'helper' => $contact->guidance_three_helper_title ?: 'What improves accuracy', 'items' => $lines($contact->guidance_three_helper_items, ['Required outcome and scope boundary', 'Users, data, and integrations', 'Target timing and decision process']), 'url' => route('quote-request'), 'link' => $contact->guidance_three_link_text ?: 'Prepare a quote request'],
    ];
@endphp
<header class="contact-hero" @if($contact->hero_banner_path)style="--hero-image:url('{{ Storage::url($contact->hero_banner_path) }}')"@endif><div class="container"><div class="row g-5 align-items-center"><div class="col-lg-7"><p class="eyebrow">{{ $heroLabel }}</p><h1>{{ $heroHeading }}</h1><p>{{ $heroDescription }}</p></div><div class="col-lg-5"><div class="response-card"><span class="response-status"><i></i>{{ $companyName }}</span><h2>{{ $contact->auto_reply_enabled && $contact->auto_reply_subject ? $contact->auto_reply_subject : 'A technical response, not a generic acknowledgement.' }}</h2><p>{{ $contact->auto_reply_enabled && $contact->auto_reply_message ? $contact->auto_reply_message : 'We review the objective, users, workflow constraints, integrations, and timing before recommending the next step. Expect an initial response within one business day.' }}</p><div class="response-actions"><a href="{{ route('demo-request') }}">{{ $contact->response_primary_cta_text ?: 'Request demo' }} <i class="bi bi-arrow-up-right"></i></a><a href="{{ route('quote-request') }}">{{ $contact->response_secondary_cta_text ?: 'Request quote' }} <i class="bi bi-arrow-up-right"></i></a></div></div></div></div></div></header>
<section class="contact-options"><div class="container"><div class="contact-option-grid">@if(filled($siteSettings['contact.email'] ?? $cmsPage->contact->email))<a href="mailto:{{ $siteSettings['contact.email'] ?? $cmsPage->contact->email }}"><i class="bi bi-envelope"></i><span>{{ $emailLabel }}</span><strong>{{ $siteSettings['contact.email'] ?? $cmsPage->contact->email }}</strong></a>@endif @if(filled($siteSettings['contact.phone'] ?? $cmsPage->contact->phone))<a href="tel:{{ preg_replace('/[^0-9+]/', '', $siteSettings['contact.phone'] ?? $cmsPage->contact->phone) }}"><i class="bi bi-telephone"></i><span>{{ $phoneLabel }}</span><strong>{{ $siteSettings['contact.phone'] ?? $cmsPage->contact->phone }}</strong></a>@endif @if($whatsApp = $socialLinks->firstWhere('platform', 'whatsapp'))<a href="{{ $whatsApp->url }}" target="_blank" rel="noopener"><i class="bi bi-whatsapp"></i><span>{{ $whatsappLabel }}</span><strong>{{ $whatsApp->url }}</strong></a>@elseif(filled($siteSettings['contact.whatsapp'] ?? $contact->whatsapp))<a href="https://wa.me/{{ preg_replace('/[^0-9]/', '', $siteSettings['contact.whatsapp'] ?? $contact->whatsapp) }}" target="_blank" rel="noopener"><i class="bi bi-whatsapp"></i><span>{{ $whatsappLabel }}</span><strong>{{ $siteSettings['contact.whatsapp'] ?? $contact->whatsapp }}</strong></a>@endif<a href="#inquiry"><i class="bi bi-chat-square-text"></i><span>{{ $inquiryLabel }}</span><strong>{{ $companyName }}</strong></a></div></div></section>
<section class="page-section contact-guidance"><div class="container"><div class="section-heading"><div><p class="eyebrow">{{ $contact->guidance_label ?: 'Prepare the conversation' }}</p><h2>{{ $contact->guidance_title ?: 'Choose the path that matches your decision.' }}</h2><p>{{ $contact->guidance_description ?: 'You do not need a finished specification. Clear operating context is more useful than a long feature list.' }}</p></div></div><div class="contact-guidance-grid">@foreach($guidance as $card)<article><span>{{ $card['number'] }}</span><h3>{{ $card['title'] }}</h3><p>{{ $card['description'] }}</p><strong>{{ $card['helper'] }}</strong><ul>@foreach($card['items'] as $item)<li>{{ $item }}</li>@endforeach</ul>@isset($card['url'])<a href="{{ $card['url'] }}">{{ $card['link'] }} <i class="bi bi-arrow-right"></i></a>@endisset</article>@endforeach</div></div></section><section class="inquiry-section" id="inquiry"><div class="container"><div class="row g-5"><div class="col-lg-7"><div class="inquiry-heading"><p class="eyebrow">{{ $contact->form_label ?: 'Tell us what you need' }}</p><h2>{{ $heroHeading }}</h2><p>{{ $contact->form_description ?: 'Include the current process, affected users, known integrations, timing constraints, and the result the business needs to measure. This helps us respond with a useful next step.' }}</p></div><x-lead-form-alerts/><form class="inquiry-form" method="post" action="{{ route('contact.store') }}">@csrf<x-lead-contact-fields :name-label="$contact->form_name_label ?: 'Name'" :company-label="$contact->form_company_label ?: 'Company'" :email-label="$contact->form_email_label ?: 'Email'" :phone-label="$contact->form_phone_label ?: 'Phone'"/><div class="mt-3"><label class="form-label" for="website">{{ $contact->website_label ?: 'Website' }} <span class="text-muted">({{ $contact->optional_label ?: 'optional' }})</span></label><input class="form-control" id="website" type="url" name="website" maxlength="2048" value="{{ old('website') }}"></div><div class="mt-3"><label class="form-label" for="subject">{{ $contact->subject_label ?: 'Subject' }}</label><input class="form-control" id="subject" name="subject" required maxlength="200" value="{{ old('subject') }}"></div><div class="mt-3"><label class="form-label" for="message">{{ $contact->message_label ?: 'Message' }}</label><textarea class="form-control" id="message" name="message" rows="7" required minlength="10" maxlength="20000">{{ old('message') }}</textarea></div><button class="btn btn-primary btn-lg mt-4">{{ $contact->submit_button_text ?: 'Send message' }} <i class="bi bi-arrow-right"></i></button></form></div><aside class="col-lg-5"><div class="office-panel"><p class="eyebrow">{{ $contact->office_label ?: 'Office information' }}</p><h2>{{ $companyName }}</h2>@if(filled($siteSettings['contact.address'] ?? null))<address>{{ $siteSettings['contact.address'] }}</address>@elseif($cmsPage->contact->address)<address>{{ $cmsPage->contact->address }}</address>@endif @if(filled($siteSettings['contact.google_maps_embed'] ?? null))<div class="office-map">{!! $siteSettings['contact.google_maps_embed'] !!}</div>@elseif($cmsPage->contact->map_embed_url)<a class="office-map" href="{{ $cmsPage->contact->map_embed_url }}" target="_blank" rel="noopener">{{ $contact->map_link_text ?: 'View location' }} <i class="bi bi-box-arrow-up-right"></i></a>@endif<h3>{{ $contact->business_hours_heading ?: 'Business hours' }}</h3><dl>@foreach($cmsPage->businessHours->sortBy('day_of_week') as $hours)<div><dt>{{ Carbon\Carbon::create()->startOfWeek()->addDays(($hours->day_of_week+6)%7)->format('l') }}</dt><dd>{{ $hours->is_closed ? $closedLabel : substr((string) $hours->opens_at,0,5).'-'.substr((string) $hours->closes_at,0,5) }}@if(filled($hours->holiday_text))<br><small>{{ $hours->holiday_text }}</small>@endif</dd></div>@endforeach</dl>
@if($socialLinks->isNotEmpty())<div class="footer-socials mt-3">@foreach($socialLinks as $link)<a href="{{ $link->url }}" target="_blank" rel="noopener noreferrer" aria-label="{{ ucfirst($link->platform) }}"><i class="bi bi-{{ $link->platform === 'x' ? 'twitter-x' : $link->platform }}"></i></a>@endforeach</div>@endif</div></aside></div></div></section>
@endsection


