@extends('installer.layout')
@php($step=3)
@section('title','Database configuration')
@section('content')
<form class="installer-card" method="post" action="{{ route('install.database.store') }}">@csrf<div class="installer-card-header"><span class="step-label">Step 3 of 8</span><h1 class="h3 mt-2 mb-2">Database configuration</h1><p class="text-secondary mb-0">Connect an empty database dedicated to this installation. Existing databases are never modified.</p></div><div class="installer-card-body"><div class="row g-3"><div class="col-md-8"><label class="form-label" for="host">Database host</label><input class="form-control" id="host" name="host" required value="{{ old('host',$database['host']) }}" autocomplete="off"></div><div class="col-md-4"><label class="form-label" for="port">Port</label><input class="form-control" id="port" name="port" type="number" min="1" max="65535" required value="{{ old('port',$database['port']) }}"></div><div class="col-12"><label class="form-label" for="database">Database name</label><input class="form-control" id="database" name="database" required value="{{ old('database',$database['database']) }}" autocomplete="off"></div><div class="col-md-6"><label class="form-label" for="username">Username</label><input class="form-control" id="username" name="username" required value="{{ old('username',$database['username']) }}" autocomplete="username"></div><div class="col-md-6"><label class="form-label" for="password">Password</label><input class="form-control" id="password" name="password" type="password" value="{{ old('password',$database['password']) }}" autocomplete="current-password"></div></div><p class="form-text mt-3 mb-0"><i class="bi bi-info-circle me-1"></i>The connection is tested before these credentials are saved.</p></div><div class="installer-card-footer"><a class="btn btn-link text-secondary text-decoration-none" href="{{ route('install.requirements') }}">Back</a><button class="btn btn-primary">Test connection and continue <i class="bi bi-arrow-right ms-1"></i></button></div></form>
@endsection
