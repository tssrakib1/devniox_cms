@extends('installer.layout')
@php($step=4)
@section('title','Application configuration')
@section('content')
<form class="installer-card" method="post" action="{{ route('install.application.store') }}">@csrf<div class="installer-card-header"><span class="step-label">Step 4 of 8</span><h1 class="h3 mt-2 mb-2">Application configuration</h1><p class="text-secondary mb-0">Set the public identity and regional defaults. A secure application key is generated automatically.</p></div><div class="installer-card-body"><div class="row g-3"><div class="col-md-6"><label class="form-label" for="name">Application name</label><input class="form-control" id="name" name="name" required maxlength="100" value="{{ old('name',$application['name']) }}"></div><div class="col-md-6"><label class="form-label" for="url">Application URL</label><input class="form-control" id="url" name="url" type="url" required value="{{ old('url',$application['url']) }}"><div class="form-text">Include https:// when SSL is enabled.</div></div><div class="col-md-6"><label class="form-label" for="timezone">Timezone</label><select class="form-select" id="timezone" name="timezone">@foreach(timezone_identifiers_list() as $timezone)<option value="{{ $timezone }}" @selected(old('timezone',$application['timezone'])===$timezone)>{{ $timezone }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label" for="currency">Currency</label><input class="form-control text-uppercase" id="currency" name="currency" required maxlength="3" value="{{ old('currency',$application['currency']) }}"></div><div class="col-md-3"><label class="form-label" for="environment">Environment</label><select class="form-select" id="environment" name="environment">@foreach(['production'=>'Production','staging'=>'Staging','local'=>'Local'] as $value=>$label)<option value="{{ $value }}" @selected(old('environment',$application['environment'])===$value)>{{ $label }}</option>@endforeach</select></div></div></div><div class="installer-card-footer"><a class="btn btn-link text-secondary text-decoration-none" href="{{ route('install.database') }}">Back</a><button class="btn btn-primary">Continue <i class="bi bi-arrow-right ms-1"></i></button></div></form>
@endsection
