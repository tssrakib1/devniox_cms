@props(['service'])
<article class="enterprise-card service-card"><a class="enterprise-card-media" href="{{ route('services.show', $service->slug) }}">@if($service->cover_image_path)<img src="{{ Storage::url($service->cover_image_path) }}" alt="{{ $service->name }}" loading="lazy" width="720" height="450">@else<div class="card-icon"><i class="bi bi-code-square" aria-hidden="true"></i></div>@endif</a><div class="enterprise-card-body"><p class="card-kicker">{{ $service->category->name }}</p><h2><a href="{{ route('services.show', $service->slug) }}">{{ $service->name }}</a></h2><p>{{ $service->short_description }}</p><a class="card-link" href="{{ route('services.show', $service->slug) }}" aria-label="Explore {{ $service->name }}">Explore service <i class="bi bi-arrow-right"></i></a></div></article>
